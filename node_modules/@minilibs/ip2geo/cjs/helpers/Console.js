'use strict';

const Console = {
    Error: (func, message) => {
        console.error(func, message);
    },
    Info: (func, message) => {
        console.info(func, message);
    },
    Warning: (func, message) => {
        console.warn(func, message);
    }
};

module.exports = Console;
