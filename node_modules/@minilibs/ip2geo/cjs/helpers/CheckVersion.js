'use strict';

var States = require('../data/States.js');
var GetNpmVersion = require('./GetNpmVersion.js');

const CheckVersion = async () => {
    if (!States.STATE.IS_CHECKED) {
        States.STATE.IS_CHECKED = true;
        await GetNpmVersion();
    }
};

module.exports = CheckVersion;
